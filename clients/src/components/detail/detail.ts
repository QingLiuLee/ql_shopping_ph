interface TabList {
    name: string;
}

interface Goods {
    title: string;
    picture: string;
}

interface Sku {
    title: string;
    picture: string;
}

interface SkuData {
    goodsId: string;
    messages: any;
    cartMessages: any;
    selectedNum: string;
}